# neutron_star_binaries

run simulation_fast.py specifying the variable MODULE of one of the following types of systems set up in the code so far: "elliptical", "circular", "periapsis", or "apoapsis."

for collisions, run collision_fast.py. 

use graphing_fast.py to adjust graphs.
